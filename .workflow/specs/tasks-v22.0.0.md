# Tasks — v22.0.0 "Alignment"

## Phase 0 — Authority and baseline

- [x] Lock the canonical V22 plan, architecture, task contract, roadmap,
  documentation index, baseline report, and active race lock.
- [x] Record current checkout, release lineage, repository inventory, routes,
  schemas, startup/resources, tests, packages, and screenshot evidence.
- [x] Keep product metadata at v21.0.0 and make no runtime behavior change.

## Phase 1 — Trust foundation

- [x] Make COPR terminal success, repository availability, clean install, and
  version readback separate mandatory workflow gates with executable tests.
- [x] Add API and daemon RPM runtime dependencies and clean-entrypoint tests.
- [x] Replace delivered Flatpak shell-like probes with structured application
  ID checks and catalog-wide regression coverage.
- [x] Implement and test bounded two-phase application resource shutdown.
- [x] Redact credential headers and colon/whitespace forms end to end.

## Phase 2 — Catalog and native alignment

- [x] Split catalog records by destination while preserving exact ordered
  projection of all 81 routes and existing compatibility behavior.
- [x] Add inert capability-state and opaque native-handoff contracts.
- [x] Add fixed, non-privileged, user-triggered Discover and KDE KCM handoffs
  with unavailable/manual fallbacks.

## Phase 3 — Visual hierarchy and workflows

- [x] Repair structural backgrounds and the shell collapse control.
- [x] Tighten Home and make Action Center expose one lifecycle-primary action.
- [x] Add Specialist Tools group overview and improve System Check and Activity
  local-state presentation without route or persistence changes.

## Phase 4 — Accessibility and Fedora 45 readiness

- [x] Extend focus, AT-SPI, geometry, scale, theme, RTL, and reduced-motion
  matrices through Home, System Check, Action Center, and Activity.
- [x] Verify provider-neutral SecretStore locked, unavailable, CRUD, and
  fallback behavior without provider-specific migration.
- [x] Preserve Fedora 44 stable and Fedora 45 preview policy unless the
  separately defined physical promotion gate passes.

## Phase 5 — Qualification and release readiness

- [x] Pass full repository, security, route, startup, compatibility, and
  packaging gates with at least 86 percent coverage.
- [x] Build and verify local candidate formats, checksums, SBOM, and
  worktree provenance; enforce artifact attestation in the release workflow
  without claiming a live attestation before an authorized commit and run.
- [x] Record remaining physical Wayland, Orca, Traditional, Atomic, install,
  and public-readback gates truthfully.

## Release-only gates

- [x] Synchronize version metadata to v22.0.0 "Alignment" only after all local
  implementation and qualification gates pass.
- [ ] Commit, push, tag, publish, install on the host, or modify remote release
  surfaces only after separate explicit authorization.
