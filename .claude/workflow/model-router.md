# Model Router

Use the default coding model for repository implementation and validation.

Prefer focused local edits for:

- Release metadata and documentation alignment.
- Service/core diagnostics.
- CLI and PyQt6 dashboard integration.
- RPM spec packaging ownership.

Use specialized review or planning agents only when explicitly requested by the user.
