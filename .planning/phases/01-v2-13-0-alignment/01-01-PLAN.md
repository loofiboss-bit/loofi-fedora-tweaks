---
phase: 01-v2-13-0-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - ALIGN-02
must_haves:
  truths:
    - "A maintainer can open README.md and see the active workflow target, latest documented release, packaged runtime baseline, and real headless API flag without contradictory claims."
  artifacts:
    - path: "README.md"
      provides: "Top-level repo entrypoint with authoritative release framing and run-mode docs"
  key_links:
    - from: "README.md"
      to: "ROADMAP.md"
      via: "Current Development Cycle section"
      pattern: "v2\\.13\\.0|Alignment"
    - from: "README.md"
      to: "loofi-fedora-tweaks/main.py"
      via: "Run Modes table"
      pattern: "--web"
---

<objective>
Reconcile the top-level README so it stops presenting `v2.11.0` as the current release and instead explains the verified repo state: active workflow target `v2.13.0`, latest documented release line `v2.12.0`, packaged runtime baseline `2.11.0`, and the real headless API flag `--web`.

Purpose: Restore trust in the first file contributors and users read.
Output: An evidence-backed `README.md` that no longer conflicts with roadmap/workflow metadata or `main.py`.
</objective>

<execution_context>
@/home/loofi/.copilot/get-shit-done/workflows/execute-plan.md
@/home/loofi/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-v2-13-0-alignment/01-CONTEXT.md
@ROADMAP.md
@.workflow/specs/.race-lock.json
@.workflow/specs/tasks-v2.13.0.md
@README.md
@CHANGELOG.md
@docs/releases/RELEASE-NOTES-v2.12.0.md
@loofi-fedora-tweaks/version.py
@pyproject.toml
@loofi-fedora-tweaks.spec
@loofi-fedora-tweaks/main.py
@loofi-fedora-tweaks.service
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace README release framing with the verified repo state</name>
  <files>README.md</files>
  <read_first>README.md; ROADMAP.md; .workflow/specs/.race-lock.json; CHANGELOG.md; docs/releases/RELEASE-NOTES-v2.12.0.md; loofi-fedora-tweaks/version.py; pyproject.toml; loofi-fedora-tweaks.spec</read_first>
  <action>Rewrite the README title/banner and "Current Development Cycle" messaging so it no longer declare `v2.11.0` as the current release. Use this exact truth model: active workflow target = `v2.13.0 "Alignment"` from `ROADMAP.md` + race-lock, latest completed documented release line = `v2.12.0 "API Migration Slice 8"` from `CHANGELOG.md` + `docs/releases/RELEASE-NOTES-v2.12.0.md`, packaged runtime/version files = `2.11.0` from `version.py` + `pyproject.toml` + `.spec`. Present these as separate facts instead of collapsing them into one "current release" label. Keep the README product description feature-neutral and stabilization-focused; do not claim `v2.13.0` is already shipped.</action>
  <acceptance_criteria>README.md does not contain `# Loofi Fedora Tweaks v2.11.0`; README.md contains `v2.13.0 "Alignment"`; README.md contains `v2.12.0 "API Migration Slice 8"`; README.md contains `2.11.0` only as packaged runtime/version-file context, not as the current release label.</acceptance_criteria>
  <verify>
    <automated>python scripts/check_release_docs.py</automated>
  </verify>
  <done>README.md communicates the repo's active slice, latest documented release, and packaged runtime baseline without contradictory "current release" language.</done>
</task>

<task type="auto">
  <name>Task 2: Correct README run-mode and API flag documentation</name>
  <files>README.md</files>
  <read_first>README.md; loofi-fedora-tweaks/main.py; loofi-fedora-tweaks.service</read_first>
  <action>Update the README run-mode table, examples, and API section to use the actual headless flag `--web` from `loofi-fedora-tweaks/main.py` and `loofi-fedora-tweaks.service`. Preserve the four modes as GUI (default), CLI (`--cli`), Daemon (`--daemon`), and Web API (`--web`). Do not rename runtime flags in code during this slice; documentation must match the existing app surface exactly.</action>
  <acceptance_criteria>README.md contains `| Web API | \`loofi-fedora-tweaks --web\``; README.md contains `loofi-fedora-tweaks --web`; README.md does not introduce `--api` as the runnable flag.</acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_main_entry.py -q</automated>
  </verify>
  <done>README.md run-mode documentation matches the actual command-line surface exposed by `main.py`.</done>
</task>

</tasks>

<verification>
Run `python scripts/check_release_docs.py` and `python -m pytest tests/test_main_entry.py -q`. The README must be grep-verifiable for `v2.13.0`, `v2.12.0`, and `--web`.
</verification>

<success_criteria>
README is the authoritative human entrypoint for the repo's current release story and run modes, without stale v2.11.0 branding or invented shipped-state claims.
</success_criteria>

<output>
After completion, create `.planning/phases/01-v2-13-0-alignment/01-01-SUMMARY.md`
</output>
