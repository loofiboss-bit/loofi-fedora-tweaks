---
phase: 01-v2-13-0-alignment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/README.md
  - docs/releases/RELEASE_NOTES.md
  - ARCHITECTURE.md
autonomous: true
requirements:
  - ALIGN-01
  - ALIGN-03
must_haves:
  truths:
    - "The docs index and latest-release index point at the real latest documented release line instead of an unrelated historical version."
    - "The architecture reference states the actual repository entry modes and no longer presents v2.4.0 as the current architecture snapshot."
  artifacts:
    - path: "docs/README.md"
      provides: "Canonical docs entrypoint"
    - path: "docs/releases/RELEASE_NOTES.md"
      provides: "Canonical latest-release index"
    - path: "ARCHITECTURE.md"
      provides: "Canonical architecture reference aligned to current repo state"
  key_links:
    - from: "docs/README.md"
      to: "docs/releases/RELEASE_NOTES.md"
      via: "latest release index pointer"
      pattern: "RELEASE_NOTES\\.md"
    - from: "ARCHITECTURE.md"
      to: "loofi-fedora-tweaks/main.py"
      via: "entry mode section"
      pattern: "--web|Four Entry Modes"
---

<objective>
Refresh the docs entrypoints and architecture reference so they point to the verified repo state: latest documented release line `v2.12.0`, active workflow target `v2.13.0`, and four actual entry modes with `--web` as the headless API flag.

Purpose: Remove contradictory release/version lore from the repo's canonical documentation surfaces.
Output: Updated `docs/README.md`, `docs/releases/RELEASE_NOTES.md`, and `ARCHITECTURE.md` with concrete, grep-verifiable target values.
</objective>

<execution_context>
@/home/loofi/.copilot/get-shit-done/workflows/execute-plan.md
@/home/loofi/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-v2-13-0-alignment/01-CONTEXT.md
@docs/README.md
@docs/releases/RELEASE_NOTES.md
@ARCHITECTURE.md
@CHANGELOG.md
@docs/releases/RELEASE-NOTES-v2.12.0.md
@ROADMAP.md
@.workflow/specs/.race-lock.json
@loofi-fedora-tweaks/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix docs entrypoints to the real latest documented release line</name>
  <files>docs/README.md, docs/releases/RELEASE_NOTES.md</files>
  <read_first>docs/README.md; docs/releases/RELEASE_NOTES.md; CHANGELOG.md; docs/releases/RELEASE-NOTES-v2.12.0.md; ROADMAP.md; .workflow/specs/.race-lock.json</read_first>
  <action>Update `docs/README.md` and `docs/releases/RELEASE_NOTES.md` so they no longer advertise `v45.0.0` or any other unrelated legacy release as current. Set the latest documented release line to `v2.12.0 "API Migration Slice 8"` and ensure the docs index calls `docs/releases/RELEASE_NOTES.md` the canonical latest-release index. Where helpful, mention `v2.13.0 "Alignment"` as the active workflow target, but do not label it as the latest shipped/documented release unless `docs/releases/RELEASE-NOTES-v2.13.0.md` exists later in the phase.</action>
  <acceptance_criteria>docs/releases/RELEASE_NOTES.md contains `## Current Release`; docs/releases/RELEASE_NOTES.md contains `v2.12.0 "API Migration Slice 8"`; docs/releases/RELEASE_NOTES.md does not contain `v45.0.0`; docs/README.md contains `Canonical latest release index: \`releases/RELEASE_NOTES.md\``.</acceptance_criteria>
  <verify>
    <automated>python scripts/check_release_docs.py</automated>
  </verify>
  <done>The docs entrypoints identify `v2.12.0` as the latest documented release and stop pointing to an unrelated historical release family.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the architecture banner and entry-mode section to match the repo</name>
  <files>ARCHITECTURE.md</files>
  <read_first>ARCHITECTURE.md; loofi-fedora-tweaks/main.py; README.md; ROADMAP.md; .workflow/specs/.race-lock.json</read_first>
  <action>Replace the stale architecture banner that still says `Version: 2.4.0 "Daemon Foundation"` with an explicit current-state banner that separates workflow and runtime facts, for example: `Workflow Target: 2.13.0 "Alignment" | Runtime Version Files: 2.11.0 | Python: 3.12+ | Framework: PyQt6 | Platform: Fedora Linux`. Rename `## Three Entry Modes` to `## Four Entry Modes` and list GUI, CLI (`--cli`), Daemon (`--daemon`), and Web API (`--web`) based on `loofi-fedora-tweaks/main.py`.</action>
  <acceptance_criteria>ARCHITECTURE.md does not contain `Version**: 2.4.0`; ARCHITECTURE.md contains `Workflow Target: 2.13.0 "Alignment"`; ARCHITECTURE.md contains `Runtime Version Files: 2.11.0`; ARCHITECTURE.md contains `## Four Entry Modes`; ARCHITECTURE.md contains `` `--web` ``.</acceptance_criteria>
  <verify>
    <automated>python scripts/check_release_docs.py</automated>
  </verify>
  <done>The architecture document reflects the real repo state and no longer encodes the outdated v2.4.0 / three-entry-mode narrative.</done>
</task>

</tasks>

<verification>
Run `python scripts/check_release_docs.py` and grep the edited files for `v2.12.0`, `v2.13.0`, `Runtime Version Files: 2.11.0`, and `## Four Entry Modes`.
</verification>

<success_criteria>
The repository's canonical documentation entrypoints all tell the same release and entry-mode story, without sending readers back to legacy release families or outdated architecture snapshots.
</success_criteria>

<output>
After completion, create `.planning/phases/01-v2-13-0-alignment/01-02-SUMMARY.md`
</output>
