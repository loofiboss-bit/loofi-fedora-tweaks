---
phase: 01-v2-13-0-alignment
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - docs/releases/RELEASE-NOTES-v2.12.0.md
  - scripts/check_release_docs.py
  - scripts/generate_workflow_reports.py
  - tests/test_release_doc_check.py
  - tests/test_generate_workflow_reports.py
  - tests/test_workflow_runner_locks.py
autonomous: true
requirements:
  - ALIGN-04
  - ALIGN-05
must_haves:
  truths:
    - "No release note in the repository claims workflow-report files that do not exist."
    - "The release-doc and workflow-report validation scripts fail when stale references or missing report files are introduced."
  artifacts:
    - path: "docs/releases/RELEASE-NOTES-v2.12.0.md"
      provides: "Evidence-backed v2.12.0 release notes"
    - path: "scripts/check_release_docs.py"
      provides: "Release-doc validator that detects missing referenced workflow reports"
    - path: "scripts/generate_workflow_reports.py"
      provides: "Workflow-report generator capable of explicit target-version artifact naming"
  key_links:
    - from: "docs/releases/RELEASE-NOTES-v2.12.0.md"
      to: ".workflow/reports/*.json"
      via: "embedded file-path claims"
      pattern: "test-results-v2\\.12\\.0|run-manifest-v2\\.12\\.0"
    - from: "scripts/check_release_docs.py"
      to: "tests/test_release_doc_check.py"
      via: "fixture-driven regression coverage"
      pattern: "missing referenced workflow report|referenced workflow artifact"
---

<objective>
Remove false `v2.12.0` workflow-report claims and harden the validation/tooling layer so the same drift cannot silently return.

Purpose: Evidence integrity matters more than optimistic release prose.
Output: Evidence-backed `v2.12.0` release notes plus stricter release-doc/workflow-report scripts and regression tests.
</objective>

<execution_context>
@/home/loofi/.copilot/get-shit-done/workflows/execute-plan.md
@/home/loofi/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-v2-13-0-alignment/01-CONTEXT.md
@docs/releases/RELEASE-NOTES-v2.12.0.md
@scripts/check_release_docs.py
@scripts/generate_workflow_reports.py
@tests/test_release_doc_check.py
@tests/test_generate_workflow_reports.py
@tests/test_workflow_runner_locks.py
@CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove or satisfy false `v2.12.0` workflow-report file claims</name>
  <files>docs/releases/RELEASE-NOTES-v2.12.0.md</files>
  <read_first>docs/releases/RELEASE-NOTES-v2.12.0.md; CHANGELOG.md; scripts/generate_workflow_reports.py</read_first>
  <action>Audit every `.workflow/reports/*.json` path named in `docs/releases/RELEASE-NOTES-v2.12.0.md`. If canonical `test-results-v2.12.0.json` and `run-manifest-v2.12.0.json` can be reconstructed from retained repo evidence, create them with canonical names and keep the path claims. Otherwise, narrow the release-note wording: replace the current `Validation Summary` bullets that name `.workflow/reports/test-results-v2.12.0.json` and `.workflow/reports/run-manifest-v2.12.0.json` with prose that summarizes the retained focused validation evidence without asserting those files exist. Do not generate current-version reports and rename them, and do not leave stale file-path claims behind.</action>
  <acceptance_criteria>Either both `.workflow/reports/test-results-v2.12.0.json` and `.workflow/reports/run-manifest-v2.12.0.json` exist, or `docs/releases/RELEASE-NOTES-v2.12.0.md` contains neither `test-results-v2.12.0.json` nor `run-manifest-v2.12.0.json`.</acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_release_doc_check.py -q</automated>
  </verify>
  <done>The v2.12.0 release note contains only evidence-backed workflow-report statements.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden report/doc validators and add target-version support to the generator</name>
  <files>scripts/check_release_docs.py, scripts/generate_workflow_reports.py, tests/test_release_doc_check.py, tests/test_generate_workflow_reports.py, tests/test_workflow_runner_locks.py</files>
  <read_first>scripts/check_release_docs.py; scripts/generate_workflow_reports.py; tests/test_release_doc_check.py; tests/test_generate_workflow_reports.py; tests/test_workflow_runner_locks.py</read_first>
  <behavior>
    - Test 1: `check_release_docs.py` fails when a release note references a missing `.workflow/reports/*.json` file.
    - Test 2: `generate_workflow_reports.py` can generate canonical filenames for an explicit target version such as `2.13.0` without editing `version.py`.
    - Test 3: workflow/race-lock tests cover active-version mismatch detection for documentation gates.
  </behavior>
  <action>Add a release-note reference scan to `scripts/check_release_docs.py` that detects `.workflow/reports/*.json` paths mentioned in release-note files and fails if the referenced files are missing. Extend `scripts/generate_workflow_reports.py` with an explicit `--version X.Y.Z` override that controls artifact filenames while preserving canonical `vX.Y.Z` naming. Update `tests/test_release_doc_check.py` with fixture cases for missing referenced workflow reports, update `tests/test_generate_workflow_reports.py` with `--version`/target-path coverage, and add any needed lock/version-mismatch assertions in `tests/test_workflow_runner_locks.py` for active-slice validation behavior.</action>
  <acceptance_criteria>scripts/check_release_docs.py contains logic that scans release-note text for `.workflow/reports/`; scripts/generate_workflow_reports.py contains `--version`; tests/test_release_doc_check.py contains a case for a missing referenced workflow report; tests/test_generate_workflow_reports.py contains a case for explicit target-version report paths.</acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_release_doc_check.py tests/test_generate_workflow_reports.py tests/test_workflow_runner_locks.py -q</automated>
  </verify>
  <done>The validation/tooling layer detects missing workflow-report references and can emit canonical artifacts for the active documentation slice.</done>
</task>

</tasks>

<verification>
Run `python -m pytest tests/test_release_doc_check.py tests/test_generate_workflow_reports.py tests/test_workflow_runner_locks.py -q` and then grep `docs/releases/RELEASE-NOTES-v2.12.0.md` for any remaining nonexistent `v2.12.0` report-file claims.
</verification>

<success_criteria>
The repo no longer contains false `v2.12.0` workflow-report claims, and future drift is blocked by script/test coverage instead of reviewer memory.
</success_criteria>

<output>
After completion, create `.planning/phases/01-v2-13-0-alignment/01-03-SUMMARY.md`
</output>
