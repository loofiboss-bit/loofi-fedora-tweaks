# V28.0.3 "Ease" — Public Release Evidence

**Status:** publication pending canonical tag workflow
**Release:** [v28.0.3 Ease on GitHub](https://github.com/loofiboss-bit/loofi-fedora-tweaks/releases/tag/v28.0.3)

This report is the release-closure record for the direct maintenance
execution fix. It is updated after the tag-driven workflow completes with the
exact commit, workflow runs, assets, checksums, attestations, COPR metadata,
and public wiki readback.

## Release identity

The release branch is `release/v28.0.3-direct-actions`. The exact release
commit and annotated tag target will be recorded here after publication. An
existing tag will never be moved.

## Automated qualification

The blocking gates are automated tests, lint, type checking, architecture and
trust-boundary checks, packaging, security analysis, dependency audit, CodeQL,
and release-document validation. Their final CI run IDs and results will be
recorded here after the canonical workflow completes.

## Supplementary evidence

Physical Fedora desktop, Polkit-agent, reboot, Atomic, keyboard, Orca, and
clean-install checks are optional supplementary evidence. Each surface is
recorded as `verified`, `pending`, or `unverified`; no rootless or offscreen
result is presented as physical qualification, and none of these surfaces
blocks publication.

## Public surfaces to read back

- GitHub tag, release, assets, checksums, attestations, and provenance
- COPR build, package metadata, signature, and result directory
- Public wiki pages and current documentation links

## Closure

This report remains incomplete until every named public surface has been read
back independently after publication.
