# v30.0.1 Steady — Local Candidate Qualification

**Run date:** 2026-09-23<br>
**Candidate state:** local, uncommitted, and unpublished<br>
**Baseline revision:** `59c6e008fd809b932dccb23e77e7912d1208b3bf`

## Automated verification

`LOOFI_IPC_MODE=disabled QT_QPA_PLATFORM=offscreen just verify` passed:

- Lint, mypy type checking, and architecture validation passed.
- **4,917 tests passed**, 40 were skipped, and 826 subtests passed.
- Repository coverage was **86.14%**, above the 85% project gate.
- Fourteen test warnings were reported; no test failed.

Focused direct coverage for `tests/test_operation_controller_v30.py` passed
32/32 tests with **92%** coverage for
`core/actions/operation_controller.py` and **90%** for
`ui/operation_worker.py`.

The following project checks passed: product contract, System Check contract,
architecture, stabilization rules, release documentation, and packaging
manifest validation. `just stats` and `just stats-check` confirmed generated
project statistics are current.

## Lifecycle and workflow checks

- Thirty real-Qt worker cycles ended with no retained QThreads. The adapter's
  owned QObject count stayed constant and returned to zero after deletion.
- Thirty utility-page route cycles confirmed first-use realization, instance
  reuse, no retained worker threads, and no growing window-owned QObject count.
- Regression coverage exercises explicit preparation and confirmation, stale
  plans, missing tools, execution timeout, malformed executor and verifier
  results, failed and lost persistence acknowledgements, pending reboot and
  resumed verification, recovery, per-item Install outcomes, Tune stop-on-error,
  independent Update sources, cancellation, overlap rejection, and close/quit
  deferral.
- Action Center remains the single authority for persistent system changes.

## RPM candidate

`just build-rpm` built the Fedora 44 noarch package
`loofi-fedora-tweaks-30.0.1-1.fc44.noarch.rpm`. `rpmbuild` completed its import
and AppStream checks, and `rpm -K` reported valid digests. Package metadata
reports `1:30.0.1-1.fc44` and the Fedora maintenance and desktop control center
summary.

The RPM payload was extracted into a temporary directory without installing
it. A temporary copy of its launcher was pointed at that extracted prefix and
successfully ran `--version`, `--cli --version`, and `--cli --help`. SHA-256:
`f0bced7aa07642d096a89633b66693f5d45b138bfd541c024e9d6ffee87e8e64`.

## Performance

The alternating ten-run before/after comparison passed the acceptance gate:
median visible Home improved **52.01%**, and median RSS decreased **5.52%**.
The full measurements and lifecycle counts are in
[`V30.0.1_PERFORMANCE.md`](V30.0.1_PERFORMANCE.md).

## Unverified and external gates

Physical KDE/GNOME desktop behavior, Polkit prompts, Atomic/bootc deployment,
reboot completion, manual keyboard operation, and Orca remain `unverified`.
GitHub CI and CodeQL, public release assets and attestations, COPR publication,
public wiki publication, and installation on a clean Fedora host were not
authorized or performed.

No host installation, package-manager transaction, reboot, commit, tag,
push, or publication was performed. This report qualifies a local candidate;
it is not public-release evidence.
